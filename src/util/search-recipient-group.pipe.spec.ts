import { SearchRecipientGroupPipe } from './search-recipient-group.pipe';

describe('SearchRecipientGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRecipientGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
