import { SearchTemplatePipe } from './search-template.pipe';

describe('SearchTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
