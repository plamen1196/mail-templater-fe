import { SearchRecipientPipe } from './search-recipient.pipe';

describe('SearchRecipientPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRecipientPipe();
    expect(pipe).toBeTruthy();
  });
});
